FROM node:14

WORKDIR /usr/src/app

COPY . .

EXPOSE 5000

RUN npm install

RUN npm install -g serve

RUN REACT_APP_BACKEND_URL=http://example.com npm run build

ENV PORT = 5000

ENV REQUEST_ORIGIN = http://example.com

CMD ["serve", "-s", "-l", "5000", "build"]