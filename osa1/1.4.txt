sudo docker run -d -it --name web ubuntu sh -c 'echo "Input website:"; read website; echo "Searching.."; sleep 1; curl http://$website;'
sudo docker exec -it web apt-get update
sudo docker attach web
helsinki.fi
